import React, { FunctionComponent } from 'react';
import { PageProps } from 'gatsby';
import { PageTitle } from '../../components/PageTitle';

const Albums: FunctionComponent<PageProps> = () => (
  <main>
    <PageTitle title="Albums" />
  </main>
);

export default Albums;
